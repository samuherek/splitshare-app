import React from 'react';
import { View, Text, SafeAreaView, StatusBar } from 'react-native';

function NewBillPage() {
  return (
    <SafeAreaView>
      <StatusBar backgroundColor="#6a51ae" />
      <Text>New bill page</Text>
    </SafeAreaView>
  );
}

export default NewBillPage;
